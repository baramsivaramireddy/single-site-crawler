package phase0

import (
	"log"
)

func Run(baseURL string) {

	log.Println(baseURL)

	// get site from command line
	// fetch the page
	// parse the page
	// build graph for page
	// save the html file text in file
	// get depth
	// limit crawler to that depth
}
